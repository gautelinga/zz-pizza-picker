---
phase: 02-app-filters
fixed_at: 2026-04-22T00:00:00Z
review_path: .planning/phases/02-app-filters/02-REVIEW.md
iteration: 1
findings_in_scope: 3
fixed: 3
skipped: 0
status: all_fixed
---

# Phase 02: Code Review Fix Report

**Fixed at:** 2026-04-22
**Source review:** .planning/phases/02-app-filters/02-REVIEW.md
**Iteration:** 1

**Summary:**
- Findings in scope: 3
- Fixed: 3
- Skipped: 0

## Fixed Issues

### WR-01: Fragile `$effect` side-channel for filter-change clearing

**Files modified:** `src/components/PizzaPicker.svelte`
**Commit:** 77a989e
**Applied fix:** Removed the `$effect` that used a bare `activeFilter;` read as an implicit dependency trigger. Replaced with an explicit `setFilter(f: Filter)` function that sets `activeFilter` and clears `result` in one place. Updated the filter button `onclick` handler from `() => (activeFilter = f.id)` to `() => setFilter(f.id)`. This is Option A from the review — idiomatic Svelte 5 with no effect needed.

### WR-02: Build fails silently if `prebuild` is skipped

**Files modified:** `src/pages/index.astro`
**Commit:** 4e30ff0
**Applied fix:** Added two comment lines above the `menu.json` import explaining that the file is generated by `npm run scrape` (via the `prebuild` hook) and that developers must use `npm run build`, not `astro build` directly. Zero code change — documentation fix as recommended.

### WR-03: `@astrojs/check` and `typescript` in `dependencies` instead of `devDependencies`

**Files modified:** `package.json`
**Commit:** 37732f3
**Applied fix:** Moved `@astrojs/check` and `typescript` out of `dependencies` and into a new `devDependencies` block. The remaining six runtime/build dependencies (`@astrojs/svelte`, `@tailwindcss/vite`, `astro`, `cheerio`, `svelte`, `tailwindcss`) stay in `dependencies`. JSON structure validated after edit.

---

_Fixed: 2026-04-22_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
