---
phase: 02-app-filters
plan: 01
subsystem: infra
tags: [astro, svelte, tailwindcss, typescript, vite]

# Dependency graph
requires:
  - phase: 01-data-pipeline
    provides: public/menu.json shape with scraped_at, source_url, pizzas array
provides:
  - Svelte 5 island framework installed and wired into Astro
  - Tailwind CSS v4 via @tailwindcss/vite Vite plugin
  - src/styles/global.css Tailwind entry point with body font rule
  - src/types/pizza.ts exporting Pizza, MenuData, and Filter shared types
affects: [02-02, 02-03]

# Tech tracking
tech-stack:
  added:
    - "@astrojs/svelte@8.0.5"
    - "svelte@5.55.4"
    - "tailwindcss@4.2.2 (exact, no caret)"
    - "@tailwindcss/vite@4.2.2 (exact, no caret)"
    - "@astrojs/check@0.9.8"
  patterns:
    - "Tailwind v4 requires only @import 'tailwindcss' — no config file, no layer directives"
    - "Svelte integration added via astro add svelte (patches config automatically)"
    - "Tailwind installed manually via npm install to avoid astro add tailwind pulling deprecated @astrojs/tailwind"
    - "Shared types in src/types/pizza.ts as single source of truth for downstream components"

key-files:
  created:
    - src/styles/global.css
    - src/types/pizza.ts
    - svelte.config.js
  modified:
    - astro.config.mjs
    - package.json
    - package-lock.json

key-decisions:
  - "tailwindcss and @tailwindcss/vite pinned to exact 4.2.2 (no caret) per threat model T-02-01 supply chain mitigation"
  - "Svelte and @astrojs/svelte kept with caret (^) ranges — Astro CLI default, acceptable for patch updates"
  - "Tailwind installed manually (npm install) — avoided npx astro add tailwind which installs deprecated @astrojs/tailwind v3 adapter"
  - "@astrojs/check added as needed dependency for npx astro check type verification"

patterns-established:
  - "Pattern: Types-only module at src/types/pizza.ts — no classes, no functions, interfaces only"
  - "Pattern: Tailwind v4 entry is a single @import line in global.css — no tailwind.config.js"

requirements-completed: [CORE-01, FILT-01]

# Metrics
duration: 12min
completed: 2026-04-22
---

# Phase 2 Plan 01: Toolchain Setup Summary

**Svelte 5 + Tailwind CSS v4 wired into Astro with pinned exact versions, shared Pizza/MenuData/Filter TypeScript types, and zero astro check errors**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-04-22T10:00:00Z
- **Completed:** 2026-04-22T10:12:00Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments

- Installed @astrojs/svelte@8.0.5 and svelte@5.55.4 via `npx astro add svelte`
- Installed tailwindcss@4.2.2 and @tailwindcss/vite@4.2.2 pinned exactly (no caret) to satisfy threat model T-02-01
- Updated astro.config.mjs to register both svelte() integration and tailwindcss() Vite plugin
- Created src/styles/global.css with `@import "tailwindcss"` entry point and body system font rule
- Created src/types/pizza.ts as single source of truth for Pizza, MenuData, and Filter types
- npx astro check passes with 0 errors, 0 warnings, 0 hints

## Task Commits

Each task was committed atomically:

1. **Task 1: Install Svelte and Tailwind v4 dependencies** - `d92b8f2` (chore)
2. **Task 2: Create Tailwind entry point and shared TypeScript types** - `7e4d8eb` (feat)

## Files Created/Modified

- `astro.config.mjs` - Added @astrojs/svelte integration and @tailwindcss/vite Vite plugin
- `package.json` - Added @astrojs/svelte, svelte, tailwindcss (exact 4.2.2), @tailwindcss/vite (exact 4.2.2), @astrojs/check
- `package-lock.json` - Updated lockfile for deterministic CI installs
- `svelte.config.js` - Generated by astro add svelte (minimal default config)
- `src/styles/global.css` - Tailwind v4 entry point: @import "tailwindcss" + body font-family
- `src/types/pizza.ts` - Exported Pizza, MenuData, Filter types matching Phase 1 scraper output

## Decisions Made

- Pinned tailwindcss and @tailwindcss/vite to exact 4.2.2 (no caret) per threat model T-02-01 supply chain mitigation. npm installed with caret by default — manually edited package.json to remove carets.
- Svelte packages kept at caret ranges (^8.0.5, ^5.55.4) — Astro CLI default, only patch updates would be applied automatically, acceptable.
- Avoided `npx astro add tailwind` which installs deprecated @astrojs/tailwind v3 adapter. Used `npm install tailwindcss @tailwindcss/vite` directly as documented in the plan.
- Added @astrojs/check as a dependency: `npx astro check` prompted for it interactively, installed via `npm i @astrojs/check typescript` to enable the verify step.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Pinned Tailwind packages to exact versions after npm installed with carets**
- **Found during:** Task 1 (Install Svelte and Tailwind v4 dependencies)
- **Issue:** `npm install tailwindcss@4.2.2 @tailwindcss/vite@4.2.2` wrote `"^4.2.2"` into package.json rather than exact `"4.2.2"`, failing acceptance criteria requiring exact pinned versions
- **Fix:** Edited package.json to remove carets from both tailwindcss and @tailwindcss/vite entries
- **Files modified:** package.json
- **Verification:** `grep -E '"tailwindcss":\s*"4\.2\.2"' package.json` matched; `grep -E '"@tailwindcss/vite":\s*"4\.2\.2"' package.json` matched
- **Committed in:** d92b8f2 (Task 1 commit)

**2. [Rule 3 - Blocking] Installed @astrojs/check to enable npx astro check**
- **Found during:** Task 2 (Create Tailwind entry point and shared TypeScript types)
- **Issue:** `npx astro check` required @astrojs/check package which was not in package.json — interactive prompt would block CI
- **Fix:** Ran `npm i @astrojs/check typescript` to install the checker dependency
- **Files modified:** package.json, package-lock.json
- **Verification:** `npx astro check` completed non-interactively with "0 errors, 0 warnings, 0 hints"
- **Committed in:** 7e4d8eb (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (1 bug/wrong version range, 1 blocking missing dependency)
**Impact on plan:** Both auto-fixes necessary for correctness and security. No scope creep.

## Issues Encountered

None beyond the two auto-fixed deviations above.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Svelte 5 island framework ready for PizzaPicker.svelte component (Plan 02)
- Tailwind v4 classes available globally via @import in global.css
- Pizza, MenuData, and Filter types importable from src/types/pizza.ts
- `npx astro check` passes — type-safe foundation for downstream Svelte/Astro components
- No blockers for Plan 02

---
*Phase: 02-app-filters*
*Completed: 2026-04-22*
